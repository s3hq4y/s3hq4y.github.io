{
  "name": "s9y-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "s9y — visual portfolio with Babylon.js 3D.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@babylonjs/core": "^9.26.2"
  }
}