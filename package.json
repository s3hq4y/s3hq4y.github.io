{
  "name": "s9y-os",
  "version": "1.0.0",
  "private": true,
  "description": "s9y OS — a Windows 10 Fluent style web operating system, written in TypeScript.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
